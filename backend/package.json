{
  "name": "iconvex-backend",
  "version": "1.0.0",
  "description": "IConveX backend API — converts IFC (BIM) files to XKT using @xeokit/xeokit-convert",
  "license": "MIT",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --watch --env-file-if-exists=.env server.js"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "@xeokit/xeokit-convert": "^1.3.2",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "multer": "^2.0.0"
  }
}
